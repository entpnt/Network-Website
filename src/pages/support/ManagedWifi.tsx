import React from "react";
import SupportPageTemplate from "../../components/SupportPageTemplate";

const ManagedWifi: React.FC = () => {
  return (
    <SupportPageTemplate pageKey="managed-wifi">
      {/* Additional custom content can be added here */}
    </SupportPageTemplate>
  );
};

export default ManagedWifi;
