import React from "react";
import SupportPageTemplate from "../../components/SupportPageTemplate";

const FiberConstructionProgress: React.FC = () => {
  return (
    <SupportPageTemplate pageKey="construction">
      {/* Additional custom content can be added here */}
    </SupportPageTemplate>
  );
};

export default FiberConstructionProgress;
